# thindots

Extracted some broader shareable dot files for things like git config/aliases
and general utility scripts from a larger dotfiles setup.

idempotent, writes backups of anythig user chooses to overwrite

## Install

```sh
./install-symlinks.sh
```
